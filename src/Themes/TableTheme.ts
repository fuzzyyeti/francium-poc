import {createTheme} from "@mui/material";

const theme = createTheme({
    palette: {
        background: {
            paper: '#727575',
        },
        text: {
            primary: '#f0f3f7',
            secondary: '#e1e2e3',
        },
    },
});

export default theme;